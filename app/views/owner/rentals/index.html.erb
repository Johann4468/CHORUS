<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Ma Page d'acceuil</title>
  <link rel="shortcut icon" href="data:image/x-icon;" type="image/x-icon">
  <!-- We need this line below for responsiveness! -->
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="../../assets/stylesheets/application.scss">
</head>
  <!-- code a Hero / Banner section -->
  <div class="banner">
    <div class="my-container">

      <p></p>

    </div>
  </div>

  <!-- code a "How it works" section -->
  <div class="my-container">
    <div class="my-cards">

          <%   @my_rentals.each do |rental| %>
      <div class="my-card">
          <%= image_tag(rental.instrument.photo)%>
          <h4><%= rental.instrument.brand.upcase  %> <%= rental.instrument.model.capitalize  %></h4>
          Prix total de la reservation :<h4> <%= rental.total_price  %> €</h4></br>
          <p style="text-align: justify"><%= rental.message_to_owner  %></p>
          <%= rental.acceptation %>

          <%= button_to "Accepter", accept_owner_rental_path(rental), method: :patch %>
          <%= button_to "Refuser", refuse_owner_rental_path(rental), method: :patch %>
      </div>
          <% end  %>

    </div>
  </div>

  <!-- code a footer -->
  <div class="footer">
    <div class="links">
      <a href="#"><i class="fab fa-fw fa-facebook-f"></i></a>
      <a href="#"><i class="fab fa-fw fa-github"></i></a>
      <a href="#"><i class="fab fa-fw fa-twitter"></i></a>
      <a href="#"><i class="fab fa-fw fa-instagram"></i></a>
      <p> Un site internet réalisé par des etudiant du @wagon Fait par nous pour nous </p>
    </div>
  </div>

</body>
</html>
